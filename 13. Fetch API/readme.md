# Fetch Api
The Fetch API provides an interfaces for fetching(sending/receiving) resources.

It uses Request and Response Objects.

The fetch() method is used to fetch a resource(data).

let promise = fetch(URL,[options])

# Understanding Terms

AJAX is Asynchronous JS & XML

JSON is JavaScript Object Notation

json() method : returns a second promise that resolves with result of parsing the response body ttext as JSON.(input is JSON,output is JS object) 

# Request & Response
HTTP Verbs

Response Status Code

HTTP response headers also contain details about the responses,such as content type,HTTP status code etc.

